<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo2.mapper.BoardMapper">

    <select id="findAll" resultType="com.example.demo2.model.Board">
        SELECT id, title, event_date, content, category1, category2, category3, image_url, created_at
        FROM boards
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO boards(title, event_date, content, category1, category2, category3, image_url)
        VALUES(#{title}, #{eventDate}, #{content}, #{category1}, #{category2}, #{category3}, #{imageUrl})
    </insert>

    <select id="findById" resultType="com.example.demo2.model.Board">
        SELECT id, title, event_date, content, category1, category2, category3, image_url, created_at
        FROM boards
        WHERE id = #{id}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM boards
    </select>

    <update id="update">
        UPDATE boards
        SET title = #{title},
            event_date = #{eventDate},
            content = #{content},
            category1 = #{category1},
            category2 = #{category2},
            category3 = #{category3},
            image_url = #{imageUrl}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM boards WHERE id = #{id}
    </delete>

</mapper>
