<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo2.mapper.TransactionMapper">

    <select id="findAll" resultType="com.example.demo2.model.Transaction">
        SELECT * 
        FROM transactions 
        ORDER BY date DESC, time DESC
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM transactions
        <where>
            <if test="searchMerchant != null and searchMerchant != ''">
                AND merchant LIKE CONCAT('%', #{searchMerchant}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
                AND date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND date &lt;= #{endDate}
            </if>
        </where>
    </select>

    <select id="findPaged" resultType="com.example.demo2.model.Transaction">
        SELECT * 
        FROM transactions 
        <where>
            <if test="searchDate != null and searchDate != ''">
                AND date LIKE CONCAT('%', #{searchDate}, '%')
            </if>
            <if test="searchMerchant != null and searchMerchant != ''">
                AND merchant LIKE CONCAT('%', #{searchMerchant}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
                AND date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY date DESC, time DESC
        LIMIT #{size} OFFSET #{offset}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO transactions (
            date, time, card, merchant, amount_krw, amount_usd, 
            payment_method, merchant_info, discount, points, 
            status, due_date, approval_no
        ) VALUES (
            #{date}, #{time}, #{card}, #{merchant}, #{amountKrw}, #{amountUsd}, 
            #{paymentMethod}, #{merchantInfo}, #{discount}, #{points}, 
            #{status}, #{dueDate}, #{approvalNo}
        )
    </insert>
    
    <select id="getSearchStatistics" resultType="map">
        SELECT 
            COALESCE(SUM(amount_krw), 0) as sumAmount,
            COALESCE(SUM(points), 0) as sumPoints,
            COUNT(*) as count
        FROM transactions
        <where>
            AND status != '승인취소'
            <if test="searchDate != null and searchDate != ''">
                AND date LIKE CONCAT('%', #{searchDate}, '%')
            </if>
            <if test="searchMerchant != null and searchMerchant != ''">
                AND merchant LIKE CONCAT('%', #{searchMerchant}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
                AND date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND date &lt;= #{endDate}
            </if>
        </where>
    </select>

</mapper>
